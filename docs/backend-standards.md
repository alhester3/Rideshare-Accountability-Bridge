# Backend Standards

- Use ASP.NET Core Web API controllers with RESTful route design.
- Return proper HTTP status codes and consistent JSON response shapes.
- Use direct SQL with `Microsoft.Data.Sqlite`; do not use ORMs.
- Keep methods single-purpose and use clear error handling paths.

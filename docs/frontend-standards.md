# Frontend Standards

- Use only vanilla JavaScript for DOM rendering and state updates.
- Render all UI inside the `#app` element.
- Prefer Bootstrap utility and component classes over custom CSS.
- Use async/await for network requests and show user-friendly error states.
